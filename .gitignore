# Licensed stock assets — do NOT commit to this PUBLIC repo (Adobe Stock license).
# Used locally by 02-photo-terrain.html; the public demo falls back to a
# procedural texture + drag-and-drop upload, so no licensed file is redistributed.
docs/**/AdobeStock_*.jpeg
docs/**/AdobeStock_*.jpg
assets/02-portrait.jpg

# Verification / tooling artifacts
.playwright-mcp/
*.local.*

# Brainstorm exports / working notes — contain personal notebook URLs (ChatGPT,
# NotebookLM) or local-machine paths. Keep local, don't publish.
docs/**/draftIdea.md
docs/**/*-inquiry.md
docs/**/STATUS.md

# 04 Emergent Portrait — heavy intermediates kept local. The repo ships only the
# optimized versions (04-portrait.glb = Draco, 04-backdrop.webp).
assets/04-portrait-source.png
assets/04-backdrop.png
assets/04-portrait.orig.glb
assets/04-portrait.fixed.glb
assets/04-backdrop-empty.src.png
