# Licensed stock assets — do NOT commit to this PUBLIC repo (Adobe Stock license).
# Used locally by 02-photo-terrain.html; the public demo falls back to a
# procedural texture + drag-and-drop upload, so no licensed file is redistributed.
docs/**/AdobeStock_*.jpeg
docs/**/AdobeStock_*.jpg
docs/**/AdobeStock_*.png
docs/**/AdobeStock_*.mov
assets/02-portrait.jpg

# Verification / tooling artifacts
.playwright-mcp/
*.local.*

# Brainstorm exports / working notes — contain personal notebook URLs (ChatGPT,
# NotebookLM) or local-machine paths. Keep local, don't publish.
docs/**/draftIdea.md
docs/**/*-inquiry.md
docs/**/STATUS.md

# 04 Emergent Portrait — heavy intermediates kept local. The repo ships only the
# optimized versions (04-portrait.glb = Draco, 04-backdrop.webp).
assets/04-portrait-source.png
assets/04-backdrop.png
assets/04-portrait.orig.glb
assets/04-portrait.fixed.glb
assets/04-backdrop-empty.src.png
assets/*.src.mp4

# 05 Emergent Life — heavy intermediates & working notes kept local. The repo ships
# only 05-emergent-life.html + assets/05-frames/*.webp + assets/05-frames/manifest.json.
# (docs/**/{STATUS,draftIdea,*-inquiry}.md are already covered by the globs above.)
assets/05-src/
docs/260614/keyframes/
docs/260615/keyframes/
docs/260615/PLAN.md
docs/260615/wan-actions.md
docs/260616/keyframes/
assets/05-frames.bak/
# 05 verification screenshots (throwaway)
05-arc-*.jpeg
05-verify-*.jpeg

# Secrets — NEVER commit. Copy .env.example -> .env and fill keys locally.
.env
.env.local
.env.*.local

# 06 Cat Skeleton — candidate GLBs from image-to-3D kept local (derived from licensed
# source images). The repo will ship only the final optimized 06 GLB + html.
docs/260618/*.glb
!docs/260618/cat_skeleton_anim.glb
docs/260618/*.blend
docs/260618/*.blend1
# 06 throwaway GLB-inspection contact sheets / browser verification screenshots
glb-*.jpeg
cat-*.png
06-*.png
cat-v*.jpeg
# 06 local-only debug page — panels reference GLBs that are gitignored above,
# so a clean checkout would 404. Keep local.
compare-skeletons.html

# Report verification screenshots (throwaway)
report-*.png

# 07 verification screenshots / exported PNGs (throwaway)
07-*.png
street_*.png

# 09 verification screenshots / scratch frames (throwaway)
flow-*.png
flow-*.raw
scratch-bgframe.png

# 10 音の磨き (260724) — Ableton エクスポート WAV・中間生成物はローカルのみ。
# 採用ワンショットは 10-neon-tunnel-rush.html に base64 で同梱済み(それが正)。
docs/260724/*.wav
docs/260724/*.asd
docs/260724/slices/
docs/260724/processed/
docs/260724/*-samples-block.txt
docs/260724/10-backup-*.html
docs/260724/review-diff*.txt

# 11 音・画像工房 (260724) — 中間生成物はローカルのみ(採用物は 11-kaleido-overdrive.html と assets/11-motifs/ が正)
docs/260724/drum-samples-block-11.txt
docs/260724/11-backup-*.html
docs/260724/motif-src/
docs/260724/motif-contact*.jpg
docs/260724/*.png
docs/260724/review-diff-11-module.js
